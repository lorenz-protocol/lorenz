# Rust
/target
**/target
Cargo.lock.bak

# Anchor / Solana
programs/executor/target
.anchor
test-ledger
**/*.so

# Secrets / local config (NEVER commit keys or live endpoints)
*.key
*.keypair.json
id.json
config.local.toml
.env
.env.*

# WASM / JS build artifacts (regenerate with `wasm-pack build`; see crates/lorenz-wasm/README.md)
crates/lorenz-wasm/pkg
crates/lorenz-wasm/pkg-node
node_modules

# Editor / OS
.DS_Store
.idea
.vscode
*.swp
