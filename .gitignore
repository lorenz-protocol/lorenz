# Rust
/target
**/target
Cargo.lock.bak

# Anchor / Solana
programs/executor/target
.anchor
test-ledger
**/*.so

# Secrets / local config (NEVER commit keys or live endpoints)
*.key
*.keypair.json
id.json
config.local.toml
.env
.env.*

# Editor / OS
.DS_Store
.idea
.vscode
*.swp
